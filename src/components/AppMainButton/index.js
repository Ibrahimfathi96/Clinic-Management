import {View, Text} from 'react-native';
import React from 'react';
import styles from './styles';

const AppMainButton = () => {
  return (
    <View style={styles.container}>
      <Text>AppMainButton</Text>
    </View>
  );
};

export default AppMainButton;
